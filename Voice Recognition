def split(s):
    user=s
    removewords = ['an','a','at','is','the']
    querywords = user.split()
    test = [word for word in querywords if word.lower() not in removewords]
    return test

print(split('Georgie is one of the best dogs ever'))
